# ──────────────────────────────────────────────
# Docker Compose（仅本地没有的基础设施）
#   MySQL → 本机 Homebrew 安装的，不需要容器
#   RabbitMQ → 本机 Homebrew 安装的，不需要容器
#   微服务 → 本地 go run，不需要容器
#
#   目前 docker-compose 为空，仅作为模板保留。
#   后续上线到 K8s 时再补充。
# ──────────────────────────────────────────────
services:
  # 暂无
